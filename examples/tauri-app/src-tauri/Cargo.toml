[package]
name = "tauri-app"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
license = ""
repository = ""
edition = "2024"
rust-version = "1.88.0"

[workspace]

[lib]
name = "tauri_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]
tauri-build = { version = "2.5.5", default-features = false, features = [] }

[dependencies]
# Keep the tauri crate on the same major/minor as the example's
# @tauri-apps/api npm package — tauri-cli hard-fails on a version mismatch
# (see build-platforms.yml). ^2.10 of the plugin crate accepts 2.11.x, so
# cargo resolves a single unified version.
tauri = { version = "2.11", features = [] }
tauri-plugin-device-ai-apis = { path = "../../../" }
tauri-plugin-mcp-bridge = "0.8.2"
